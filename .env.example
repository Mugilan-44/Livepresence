# Root configuration lives with the app that consumes it.
# Copy these files locally; never commit their populated versions:
#   frontend/.env.example -> frontend/.env.local
#   backend/.env.example  -> backend/.env.local
